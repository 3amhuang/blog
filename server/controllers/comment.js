import models from '../models'
import { listData, success, badRequest, serverError } from '../utils'
import Sequelize from 'sequelize'

const Op = Sequelize.Op

class CommentController {
  async find (ctx) {
  }

  async create (ctx) {
  }

  async delete (ctx) {
  }
}

export default new CommentController()
